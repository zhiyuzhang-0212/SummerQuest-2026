# A0 公开提交：王文煊

> 本文件公开可见，只保留脱敏后的状态和结论。命令原始输出及组内核验材料不进入 Git 历史。

## GitHub 与 PR

- 分支：`a0/wx9Songs`
- Git 操作总结：已完成课程仓库 Fork，配置官方仓库为 `upstream`，并从最新 `upstream/main` 创建干净分支。本次只修改 `students/王文煊/`，使用 Conventional Commits 提交并向上游 `main` 发起 Pull Request。

## Linux 环境摘要

- 操作系统：Ubuntu 24.04
- Python：3.12.3
- 用户、CPU、内存和磁盘状态：已完成基本检查；公开报告不记录账号、硬件型号、容量或挂载路径
- Virtual environment：已在用户级目录创建
- 模拟敏感配置文件权限：`600`
- 常驻进程方式：`tmux`

公开记录不包含用户名、主机名、IP、内部路径、SSH 配置、完整命令行或其他用户的进程信息。

## GPU 状态检查

### `nvidia-smi`

- Exit code：0
- 状态类别：成功
- 公开输出：不提交设备名称或资源状态明细

### `gpustat`

- 安装版本：1.1.1
- Exit code：0
- 状态类别：成功
- 公开输出：不提交设备名称或资源状态明细

### 状态解释

`nvidia-smi` 成功返回，说明当前环境中的 NVIDIA 驱动与 NVML 查询链路可用。`gpustat` 已安装在用户级 Python virtual environment 中，并能通过 NVML 正常查询状态。

公开记录只保留退出码、状态类别和必要结论，不记录具体显卡名称、型号、数量、容量、UUID、利用率、温度或进程信息。本次检查未使用 `sudo` 安装驱动，也未修改系统级环境。

## 飞书补充文档

- 链接：https://fudan-nlp.feishu.cn/wiki/LX58w9MjRiGMSrkZ9vac9jAKnKu
- 权限状态：组织内公开，且未开启互联网公开访问

该文档只保存助教核验所需的最小脱敏记录，包括 Linux 检查完成情况、两个 GPU 命令的退出码、已脱敏的关键结果和排查结论。

## 问题与收获

- 认识到公开仓库中的敏感信息一旦进入 commit，后续新增删除 commit 不能清除已有历史，因此重新提交必须从最新上游主分支建立干净历史。
- 学会区分系统级 GPU 查询工具与用户级 Python 工具：两者都依赖可用的驱动、NVML 和设备访问条件。
- 日志不能直接作为公开证据；应先提取退出码和状态类别，再删除主机名、路径、设备明细和进程信息。
- Python 包应安装在用户级 virtual environment 中，不使用 `sudo pip`，也不为完成检查修改系统级驱动。

## 自检

- [x] 我实际运行了 `nvidia-smi` 和 `gpustat`，并记录了退出码。
- [x] 我没有为了 GPU 检查使用 `sudo` 安装驱动或修改系统环境。
- [x] 公开内容已删除用户名、主机名、IP、内部路径、进程参数、具体设备信息和组内数据。
- [x] GitHub 内容不含任何 Secret、Token、Cookie、密码或私钥。
- [x] 飞书个人主页和 A0 补充文档已设置为组织内公开，且没有开启互联网公开访问。
- [x] 我已填写 A0 完成登记表。
